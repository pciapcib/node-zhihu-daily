#! /usr/bin/env node

const program = require('commander')

const { version } = require('../package')

program
  .version(version)
  .usage(' ')
  .command('run', 'run main menu').alias('r')
  .command('latest', 'fetch latest news').alias('l')
  .parse(process.argv)
